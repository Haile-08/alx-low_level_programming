#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * @argc: amount of command line arg
 * @argv: array of the commadn line arg
 *
 * Description: prints the number of arguments passed into it
 *
 * Return: 0
 */

int main(int argc, char  *argv[])
{
	printf("%d\n", argc);
	(void)argv;
	return (0);
}
