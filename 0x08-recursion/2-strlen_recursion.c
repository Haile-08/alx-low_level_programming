#include "main.h"

/**
 * _strlen_recursion - returns the length of a string
 * @s: the string passed with funciton
 * Return: length of a string
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
